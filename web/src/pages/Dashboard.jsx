export default function Dashboard(){
  return (
    <div className="container">
      <div className="card">
        <h2>Dashboard</h2>
        <p>Use the top navigation to explore auctions, manage your wallet, set favorites, or (if a farmer) create auctions.</p>
      </div>
    </div>
  );
}
